import numpy as np

max=30
gen=0
Axiom: R(5,max,gen)

derivation length: 10
production:
R(x,max,gen):
  if x>60:
    ang = np.random.random()*(-2*max)+max
    y = np.random.randint(100)
    produce F(1) ^(ang) R(y,max,gen+1)
  else:
    new_max=max
    ang = np.random.random()*(-2*max)+max
    y = np.random.randint(100)
    z = np.random.randint(100)
    produce  R(z,new_max,gen+1) F(1) [^(ang) R(y,max,gen+1)]
  


interpretation:


endlsystem